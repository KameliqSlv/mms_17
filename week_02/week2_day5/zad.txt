1 Създайте следната функция
char* toUpper(const char* str)

Функцията заделя динамична памет.
Копира символите от стр, като
* малките латински букви ги прави с главни латински букви

Функцията връща указател към новия символен низ(в динами1нара памет)

2.Създайте функцията genRandom;

int* genRandom (size_t elem_c)

funkciqra zadekq dinami`en masiw
функцията задава случайни стойностти на екементите в интервала ш-100 ; +100щ
функцията връща указател към новозаделения масив

3. Суздайте функция genRandom

gen Random (void(*init_elem)(void*), size_t elem_c, size_t elem_size);

функцията заделя динамичен масив , който може да се побере елем–ц на брой елементи
всеки елемент е с размер елем–сизе
началните стойности на всеки елемент на масива се задават с функцията инит–ел
функцията връща указател към заделения масив

void(init_int(void *int_adress)){
    (int*)(int _adr) = 23;
}

int arr = genArray (init_int, 99, sizeof(int), )
arr = {23,23,23,23,23 ...}